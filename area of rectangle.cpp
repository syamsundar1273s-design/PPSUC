#include <stdio.h>

int main()
{
    int l,b;
    float area;
    printf("Enter l,b values");
    scanf("%d%d",&l,&b);
    area=l*b;
    printf("Area of Rectangle:%f",area);

return 0;
}
